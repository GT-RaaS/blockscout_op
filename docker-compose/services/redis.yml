version: '3.9'

services:
  redis-db:
    image: 'redis:alpine'
    container_name: redis-db
    command: redis-server
    volumes:
      - /home/ubuntu/work/blockscout_op/docker-compose/redis-data:/data
